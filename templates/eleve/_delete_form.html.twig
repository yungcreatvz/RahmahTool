<form method="post" class="d-inline-flex" action="{{ path('eleve_delete', {'id': eleve.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eleve.id) }}">
    <button class="btn btn-primary btn">Supprimer</button>
</form>
