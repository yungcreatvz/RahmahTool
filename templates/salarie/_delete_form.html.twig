<form method="post" action="{{ path('salarie_delete', {'id': salarie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salarie.id) }}">
    <button class="btn btn-primary btn-lg">Delete</button>
</form>
