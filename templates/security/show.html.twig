{% extends 'dashboardbase.html.twig' %}
{% block title %}Base des Parents {% endblock %}
    {% block stylesheets %}
        <link href="\vendor/fontawesome-free/css/all.min.css" rel="stylesheet" type="text/css">
        <link href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i" rel="stylesheet">
        <!-- Custom styles for this template-->
        <link href="\css/sb-admin-2.min.css" rel="stylesheet">
    {% endblock %}
{% block body %}
    {%  if utilisateur.id %}
        <div class="container shadow col-6">
           <span class="align-content-center m-5"> <h2 class="text-secondary">profil utilisateur</h2> </span>

            <table class="table ">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisateur.id }}</td>
            </tr>
            <tr>
                <th>Matricule</th>
                <td>{{ utilisateur.Prenom }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ utilisateur.Nom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ utilisateur.Email }}</td>
            </tr>

            </tr>
        </tbody>

        </table>
    <div class="row container ">
    <a class="btn btn-primary btn mr-2" href="{{ path('u_index') }}">back to list</a>

    <a class="btn btn-primary btn mr-2" href="{{ path('u_edit', {'id': utilisateur.id}) }}">edit</a>
        {{ include('security/_delete_form.html.twig') }}
        <br>
    </div>
        </div>

    {% endif %}

{% endblock %}
